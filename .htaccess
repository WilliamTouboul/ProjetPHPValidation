RewriteEngine On

# Si le fichier/dossier existe, Apache le sert normalement
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Sinon, tout part vers index.php
RewriteRule ^ index.php [L]
